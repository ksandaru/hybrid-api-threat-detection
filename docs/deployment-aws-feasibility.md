# AWS Deployment Feasibility (written-only, Phase 10)

TODO (Phase 10): describe how this stack would deploy to AWS if
productionised - API/ML as containers on ECS Fargate behind an Application
Load Balancer, RDS PostgreSQL, models in S3, logs in CloudWatch - and why
local Docker was chosen instead for the controlled latency evaluation in this
dissertation. No code, no AWS account, no cost incurred.
