# Phase 9 — Resume Notes for Claude Code

Status: **Not started.** Depends on Phase 7 (full stack running), Phase 8
(attack/benign traffic generators), and a ModSecurity/OWASP CRS container
added as an external baseline (not yet part of `docker-compose.yml`).

## Facts to reuse from earlier phases

- ATRDF 2023 is already downloaded at `datasets/raw/atrdf_2023/` (Phase 1)
  — remember it must never have been used in training (Phase 4), only
  here for cross-dataset generalisation.

## When this phase is done

Fill in `IMPLEMENTATION.md`, `MEMORY.md`, `FILES.md`, update status here.
