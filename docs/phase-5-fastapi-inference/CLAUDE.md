# Phase 5 — Resume Notes for Claude Code

Status: **Not started.** Hard dependency on Phase 4's saved `.pkl` models
and `feature_order.json`.

## Facts to reuse from earlier phases

- `ml/Dockerfile` already exists (Phase 0) targeting `python:3.13-slim` —
  do not recreate it with `python:3.11-slim` from the original spec text.

## When this phase is done

Fill in `IMPLEMENTATION.md`, `MEMORY.md`, `FILES.md`, update status here.
